<%= link_to(image_tag("#{@user[0].avatar.url}",:width => 250,:height=> 250),"#{@user[0].avatar.url}", :class => "top_up") %>
<% @random_users.each do |user| %>
<%= user.name %> <%= radio_button "","","#{user.id}" %>
<% end %>